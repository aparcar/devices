$id: https://openwrt.org/schema/cpu.yaml

properties:
  model:
    title: Model
    type: string
    enum:
      - Placeholder CPU. Run merge script

  cores:
    title: Number of Cores
    type: number
    multipleOf: 1.0
    minimum: 1

  mhz:
    title: MHz
    type: number
    multipleOf: 1.0
    minimum: 1
